# 小米摄像机洗漱行为监督系统 MVP Spec

## 1. 文档信息

**项目名称**：洗漱行为监督系统（MVP）
**部署环境**：Mini PC（Windows/Linux 常开主机，24 小时运行）
**摄像设备**：小米智能摄像机 3
**时区**：Asia/Shanghai（北京时间）
**推送渠道**：Telegram Bot
**分析窗口**：

- 早晨：06:30–07:30
- 晚上：21:30–22:30

------

## 2. 项目目标

构建一套最小可用系统，对洗漱台前的视频进行定时分析，在固定时间窗内判断：

1. 是否完成刷牙
2. 是否完成洗脸
3. 对刷牙和洗脸分别给出评级：`未完成 / 差 / 中 / 好`
4. 在每个时间窗结束后，通过 Telegram 自动推送结果

系统优先目标是**稳定运行、可验证、便于迭代**，而不是追求最高视觉精度。

------

## 3. MVP 范围

### 3.1 本期必须实现

- 使用小米智能摄像机 3 作为唯一视频源
- 通过桥接方案将视频转成程序可读取的标准流
- 系统仅在两个固定时间窗内进行分析
- 每个时间窗输出 1 次结果
- 每次结果包含：
  - 刷牙评级
  - 洗脸评级
  - 简短解释
  - 至少 1 张关键帧
- 结果通过 Telegram Bot 推送
- 本地保存日志、关键帧、关键片段

### 3.2 本期不做

- 不做微信推送
- 不做飞书推送
- 不做邮件推送
- 不做实时秒级告警
- 不做多摄像头支持
- 不做云端全量视频分析
- 不做复杂身份识别
- 不做人脸识别实名化

------

## 4. 用户故事

### 4.1 核心用户故事

作为家长，我希望系统在每天早晚两个固定时间窗自动检查孩子是否认真刷牙和洗脸，并在时间窗结束后收到一条 Telegram 通知，这样我无需手动查看米家录像，也能知道孩子是否完成洗漱。

### 4.2 结果示例

**今日早晨洗漱结果（06:30–07:30）**

- 刷牙：**好**
- 洗脸：**中**
- 说明：检测到约 94 秒连续刷牙动作，并有漱口；检测到洗脸动作，但搓洗持续时间偏短。
- 附件：关键帧 2 张，证据片段 1 段

**今日晚上洗漱结果（21:30–22:30）**

- 刷牙：**未完成**
- 洗脸：**差**
- 说明：未检测到明确刷牙行为；检测到短时湿脸和擦拭动作，但流程不完整。

------

## 5. 硬件与运行约束

### 5.1 现有硬件

Mini PC：

- CPU：AMD Ryzen 5 3500U
- 内存：16 GB
- 显卡：无独显
- 磁盘：约 477 GB SSD
- 运行方式：24 小时开机

### 5.2 约束

- 无独立 GPU，不适合重型视频大模型推理
- 必须采用 **CPU 友好型** 方案
- 应优先使用：
  - 低帧率抽样
  - ROI 裁剪
  - 规则引擎
  - 关键片段复核

------

## 6. 总体架构

```text
小米智能摄像机 3
        │
        ▼
Micam / 视频桥接服务
        │
        ▼
标准视频流（RTSP）
        │
        ▼
分析服务（Python）
 ├─ 调度器
 ├─ 拉流与抽帧
 ├─ ROI 裁剪
 ├─ 行为规则识别
 ├─ 评分引擎
 ├─ 关键帧/片段生成
 ├─ SQLite 落库
 └─ Telegram 推送
```

------

## 7. 模块设计

## 7.1 视频桥接模块

### 目标

将小米智能摄像机 3 的视频转换为可由本地程序消费的标准流。

### 输入

- 小米摄像机账号与设备接入信息

### 输出

- 本地可访问的 RTSP 地址

### 要求

- 桥接服务支持自动重连
- 主程序只依赖 RTSP，不直接依赖米家 App
- 主程序与桥接模块解耦

### 失败策略

- 若桥接失败，时间窗分析任务直接标记为 `采集失败`
- 仍然发送 Telegram 告警

------

## 7.2 调度模块

### 目标

根据北京时间，仅在两个时间窗内执行分析。

### 时间窗

- `morning`: 06:30–07:30
- `evening`: 21:30–22:30

### 调度行为

- 开始前 1 分钟检查视频流可用性
- 到达开始时间后启动录制与分析
- 时间窗结束后生成结果并推送

### 时区要求

- 强制使用 `Asia/Shanghai`
- 不允许使用系统默认时区替代

------

## 7.3 视频采集模块

### 目标

在时间窗内拉取视频并生成分析素材。

### 采样策略（MVP）

- 原始流持续读取
- 分析帧率：1–2 FPS
- 关键片段缓存：滚动保留最近 15–30 秒
- 时间窗结束后仅保留：
  - 关键帧
  - 证据短视频（5–10 秒）
  - 当日结果日志

### 存储策略

- 原始整窗视频：可选，默认关闭
- 关键片段保存 7 天
- 关键帧保存 7 天
- 日志保存 90 天

------

## 7.4 ROI 配置模块

### 目标

仅分析洗漱台相关区域，减少误判与 CPU 消耗。

### ROI 类型

- 水池区域
- 镜前站立区域
- 上半身活动区域

### 配置方式

使用配置文件定义：

- 多边形坐标
- 关键点区域
- 嘴部附近参考区域
- 面部擦洗参考区域

### 要求

- ROI 必须可人工调整
- 首次部署时通过截图人工标定

------

## 7.5 行为识别模块

### 目标

识别时间窗内是否发生刷牙和洗脸。

### 输入

- 抽样帧
- ROI 区域
- 时间序列特征

### 输出

- `toothbrushing_detected: true/false`
- `facewashing_detected: true/false`
- 行为时长估计
- 关键时刻时间戳

### MVP 检测思路

#### A. 刷牙判定特征

- 人在洗漱区持续驻留
- 手部频繁接近口部区域
- 口部附近出现连续重复动作
- 总持续时间达到阈值
- 可选：检测牙刷样细长物体
- 可选：检测漱口动作

#### B. 洗脸判定特征

- 双手接近面部区域
- 面部擦洗动作具有连续性
- 动作持续时间达到阈值
- 可选：检测湿脸后的重复擦洗

### 检测输出格式

```json
{
  "window": "morning",
  "presence_seconds": 312,
  "toothbrush_motion_seconds": 96,
  "facewash_motion_seconds": 28,
  "rinse_detected": true,
  "confidence_toothbrush": 0.82,
  "confidence_facewash": 0.64
}
```

------

## 7.6 评分引擎

### 目标

将行为识别结果转成家长可理解的评级。

### 刷牙评级规则（MVP）

#### 未完成

满足任一条件：

- 明确刷牙动作时长 < 20 秒
- 未检测到稳定口部刷动

#### 差

满足全部条件：

- 20 秒 ≤ 明确刷牙动作时长 < 45 秒
- 动作断续明显
- 未检测到漱口或仅短暂漱口

#### 中

满足全部条件：

- 45 秒 ≤ 明确刷牙动作时长 < 90 秒
- 动作基本连续
- 覆盖区域一般

#### 好

满足全部条件：

- 明确刷牙动作时长 ≥ 90 秒
- 动作连续
- 存在区域切换特征
- 存在漱口或结束整理动作

### 洗脸评级规则（MVP）

#### 未完成

满足任一条件：

- 未检测到明确双手洗脸动作
- 明确洗脸动作时长 < 8 秒

#### 差

满足全部条件：

- 8 秒 ≤ 明确洗脸动作时长 < 15 秒
- 仅短时湿脸或短时擦拭

#### 中

满足全部条件：

- 15 秒 ≤ 明确洗脸动作时长 < 30 秒
- 有连续擦洗动作，但持续偏短

#### 好

满足全部条件：

- 明确洗脸动作时长 ≥ 30 秒
- 存在湿脸、搓洗、冲净/擦干中的多数步骤

### 文案生成规则

- 不输出模型术语
- 直接输出家长可读解释
- 单条解释不超过 60 字

------

## 7.7 证据生成模块

### 目标

为结果提供可人工复核的证据。

### 输出

- 关键帧：1–3 张
- 证据片段：5–10 秒

### 选择规则

- 选择刷牙/洗脸动作最明显的片段
- 若判定为未完成，则选取最接近但不充分的动作片段，或发送“无有效证据片段”

------

## 7.8 结果存储模块

### 数据库

SQLite

### 表结构建议

#### `analysis_results`

- `id`
- `date`
- `window`
- `start_time`
- `end_time`
- `toothbrush_grade`
- `facewash_grade`
- `toothbrush_seconds`
- `facewash_seconds`
- `rinse_detected`
- `summary_text`
- `evidence_frame_path`
- `evidence_clip_path`
- `status`
- `created_at`

#### `system_events`

- `id`
- `event_type`
- `message`
- `level`
- `created_at`

### 状态字段

`status` 允许：

- `success`
- `stream_unavailable`
- `analysis_failed`
- `notify_failed`

------

## 7.9 Telegram 推送模块

### 推送时机

- 早晨时间窗结束后立即推送
- 晚上时间窗结束后立即推送

### 推送对象

- 单个 Telegram Chat ID

### 推送内容

- 文本摘要
- 关键帧图片
- 证据短视频（可选）

### 文本模板

```text
【今日早晨洗漱结果】
时间窗：06:30–07:30
刷牙：好
洗脸：中
说明：检测到约 94 秒连续刷牙动作，并有漱口；检测到洗脸动作，但搓洗时间偏短。
```

### 异常模板

```text
【洗漱系统异常】
时间窗：早晨 06:30–07:30
状态：视频流不可用
说明：本次未完成分析，请检查桥接服务或摄像机在线状态。
```

------

## 8. 非功能需求

### 8.1 性能

- 单时间窗分析任务应在时间窗结束后 3 分钟内完成推送
- CPU 峰值长期不应导致系统失去响应
- 程序异常退出后应可自动重启

### 8.2 稳定性

- 连续运行 7 天不出现严重内存泄漏
- 视频流中断时系统需记录日志
- 通知失败时需重试 1–3 次

### 8.3 隐私与安全

- 仅采集洗漱台可见区域
- 避免覆盖淋浴和如厕区域
- 本地保存视频证据，不默认上传云端
- Telegram 推送内容尽量精简
- 关键片段自动过期删除

### 8.4 可维护性

- 所有阈值放入配置文件
- ROI 可配置
- 时间窗可配置
- 推送目标可配置

------

## 9. 配置文件设计

建议使用 `config.yaml`。

```yaml
timezone: Asia/Shanghai

windows:
  - name: morning
    start: "06:30"
    end: "07:30"
  - name: evening
    start: "21:30"
    end: "22:30"

stream:
  rtsp_url: "rtsp://127.0.0.1:8554/xiaomi_cam"
  reconnect_retries: 3

analysis:
  sample_fps: 2
  roi_enabled: true
  toothbrush_min_seconds_done: 20
  toothbrush_min_seconds_medium: 45
  toothbrush_min_seconds_good: 90
  facewash_min_seconds_done: 8
  facewash_min_seconds_medium: 15
  facewash_min_seconds_good: 30

storage:
  frames_dir: "./data/frames"
  clips_dir: "./data/clips"
  db_path: "./data/db/app.db"
  retention_days_frames: 7
  retention_days_clips: 7
  retention_days_logs: 90

telegram:
  enabled: true
  bot_token: "${TELEGRAM_BOT_TOKEN}"
  chat_id: "${TELEGRAM_CHAT_ID}"
```

------

## 10. 项目目录结构

```text
project/
├─ docker-compose.yml
├─ .env
├─ config.yaml
├─ app/
│  ├─ main.py
│  ├─ scheduler.py
│  ├─ stream_reader.py
│  ├─ roi.py
│  ├─ detector.py
│  ├─ scoring.py
│  ├─ notifier_telegram.py
│  ├─ db.py
│  ├─ retention.py
│  └─ utils/
├─ data/
│  ├─ frames/
│  ├─ clips/
│  ├─ db/
│  └─ logs/
└─ docs/
   └─ roi_marking.md
```

------

## 11. Docker 部署要求

### 11.1 服务划分

#### service 1: `micam`

职责：桥接小米摄像机视频流。

#### service 2: `analyzer`

职责：执行调度、分析、评分、推送。

### 11.2 运行要求

- 设置自动重启策略
- 映射数据目录到宿主机
- `analyzer` 必须等待视频流可访问后再启动

------

## 12. 核心处理流程

### 12.1 正常流程

1. 调度器到达时间窗
2. 检查 RTSP 是否可用
3. 拉流并按 1–2 FPS 抽样
4. 对 ROI 区域做行为分析
5. 统计刷牙/洗脸动作时长与连续性
6. 评分引擎输出等级
7. 生成关键帧和证据片段
8. 落库
9. 发送 Telegram
10. 清理过期文件

### 12.2 异常流程

1. 时间窗开始前检测不到视频流
2. 记录异常事件
3. 写入失败状态
4. 发送异常 Telegram 消息
5. 不进行后续分析

------

## 13. MVP 验收标准

### 13.1 功能验收

- 能在两个固定时间窗自动触发
- 每个时间窗都能输出刷牙与洗脸两个结果
- 能通过 Telegram 收到通知
- 能查看关键帧
- 能在 SQLite 中查到记录

### 13.2 识别验收

以人工复核为准，抽样 20 次：

- 是否完成判断准确率目标：≥ 80%
- 差/中/好分级主观一致率目标：≥ 70%

### 13.3 稳定性验收

- 连续运行 7 天
- 无致命崩溃
- 至少 95% 的时间窗能正常给出结果或异常原因

------

## 14. 实施阶段表

## Phase 0：接流验证

目标：证明小米摄像机可被稳定桥接为 RTSP。

任务：

- 部署 Micam
- 获取 RTSP 地址
- 使用播放器验证可播放
- 连续运行 24 小时观察掉流情况

产出：

- 可用 RTSP URL
- 接流稳定性结论

## Phase 1：分析主链路

目标：跑通时间窗调度、抽帧、落库、Telegram。

任务：

- 建立 Python 项目骨架
- 实现调度器
- 实现拉流与抽帧
- 实现 Telegram 文本推送
- 实现 SQLite 落库

产出：

- 能收到“时间窗分析完成”通知

## Phase 2：规则引擎上线

目标：实现刷牙/洗脸判断。

任务：

- ROI 标定
- 刷牙规则实现
- 洗脸规则实现
- 阈值外置到配置

产出：

- 能输出 未完成 / 差 / 中 / 好

## Phase 3：证据附件

目标：提升可复核性。

任务：

- 关键帧生成
- 短视频截取
- Telegram 图片/视频推送

产出：

- 每次结果都可人工复核

## Phase 4：调参与稳定化

目标：减少误判，保证 7 天稳定运行。

任务：

- 连续测试
- 手工复核结果
- 调整时长阈值
- 调整 ROI
- 增加异常重试

产出：

- MVP 可长期运行版本

------

## 15. 风险与应对

### 风险 1：小米桥接不稳定

应对：

- 单独做 24 小时稳定性测试
- 增加自动重连
- 桥接失败时发送异常通知

### 风险 2：CPU 不足导致处理延迟

应对：

- 将分析帧率限制为 1–2 FPS
- 缩小 ROI
- 只保存关键片段

### 风险 3：误把驻留当洗漱

应对：

- 引入动作连续性阈值
- 引入口部/面部接触特征
- 提高“完成”门槛

### 风险 4：通知成功但识别解释不清楚

应对：

- 先做固定模板文案
- 后期再引入多模态复核

------

## 16. 后续迭代方向（非 MVP）

- 邮件日报
- 飞书机器人备用推送
- Web 管理后台
- 多模态模型复核
- 周报/月报统计
- 多摄像头支持
- 自定义家长评分规则

------

## 17. 结论

本 MVP 的核心原则是：

1. **先解决接流问题**
2. **先用规则引擎跑通闭环**
3. **先做到稳定可用，再追求更聪明**

对于当前这台 Mini PC，最合适的路线是：

**小米摄像机 3 → Micam 桥接 → Python 定时分析 → Telegram 推送**

这条路线实现成本最低，最适合先做出第一版可运行系统。